#include<STDIO.H>
//* C Program for the maching and extrating the target valve
int main(int argc, char const *argv[])
{
    
    int su[10];
    for (int i = 0; i < 5; i++)
    {
       printf("Enter Element of the array at  %d",i);
    scanf("%d",&su[i]);
    }
    int t=15;
    int sum=0;
    for (int i = 0; i < 5; i++)
    {
        if (sum<t)
        {
            /* code */
        }
        
    }
    
    return 0;
}
